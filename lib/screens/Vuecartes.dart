// ignore_for_file: prefer_const_constructors

import 'package:flutter/material.dart';

class VueCartes extends StatelessWidget {
  // Modification ici
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Vue Cartes'), // Modification ici
    );
  }
}
